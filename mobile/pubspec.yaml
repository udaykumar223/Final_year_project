name: smartcrop_ai
description: "SmartCrop AI - AI-powered crop disease detection for farmers"
publish_to: 'none'
version: 0.3.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # HTTP & API
  http: ^1.2.0
  http_parser: ^4.1.0

  # Image handling
  image_picker: ^1.1.0

  # Typography
  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
